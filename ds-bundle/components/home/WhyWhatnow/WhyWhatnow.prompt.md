WhyWhatnow from whatnow-web. Use via `window.WhatNow.WhyWhatnow` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Default

```jsx
() => (
  <LanguageProvider>
    <WhyWhatnow />
  </LanguageProvider>
)
```
