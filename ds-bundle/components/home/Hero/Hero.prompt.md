Hero from whatnow-web. Use via `window.WhatNow.Hero` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Default

```jsx
() => (
  <LanguageProvider>
    <Hero />
  </LanguageProvider>
)
```
