FeaturedGuides from whatnow-web. Use via `window.WhatNow.FeaturedGuides` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Default

```jsx
() => (
  <LanguageProvider>
    <FeaturedGuides />
  </LanguageProvider>
)
```
