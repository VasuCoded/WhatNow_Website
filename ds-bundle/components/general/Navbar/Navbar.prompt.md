Navbar from whatnow-web. Use via `window.WhatNow.Navbar` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Default

```jsx
() => (
  <LanguageProvider>
    <Navbar />
  </LanguageProvider>
)
```
