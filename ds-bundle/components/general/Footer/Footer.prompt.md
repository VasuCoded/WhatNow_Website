Footer from whatnow-web. Use via `window.WhatNow.Footer` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Default

```jsx
() => (
  <LanguageProvider>
    <Footer />
  </LanguageProvider>
)
```
